/*
CSCI 104: Homework 1 Problem 1

Use this file to test your split.cpp file.

Create linked lists and split them with your split() function.

You can compile this file like this:
g++ split.cpp test_split.cpp -o test_split
*/

#include "split.h"
#include <iostream>

using namespace std;

int main(int argc, char* argv[])
{
// 	Node* oddlist = nullptr;
// 	Node* evenlist = nullptr;


	// Node* nodelast = new Node(12, nullptr);
	// Node* nodefour = new Node(7, nodelast);
	// Node* nodethree = new Node(5, nodefour);
	// Node* nodetwo = new Node(1, nodethree);
	// Node* headnode = new Node(2, nodetwo);

	// split(headnode, oddlist, evenlist);
	// cout << "evens: " << endl;
	// while(evenlist != nullptr){
	// 	cout << evenlist->value << endl;
	// 	evenlist = evenlist->next;
	// }

	// cout << "odds: " << endl;
	// Node* tempodditer = oddlist;
	// while(tempodditer != nullptr){
	// 	cout << tempodditer->value << endl;
	// 	tempodditer = tempodditer->next;
	// }

}
